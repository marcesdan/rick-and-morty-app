import React, { memo } from 'react';
import { Grid } from '@material-ui/core';

const CharactersIndex = () => (
  <Grid
    container
    component="main"
    style={{ height: '100vh' }}
  >
  </Grid>
);

export default memo(CharactersIndex);
